You are editing a single-file React app: App.jsx. It is large. You MUST make minimal, surgical edits using SEARCH → INSERT steps. Do NOT refactor.

ABSOLUTE RULES:
- React only. No new libraries.
- Do NOT change backend routes, payloads, Firestore logic, or existing UI sections.
- Do NOT rename existing state/functions (items, user, setActiveTab, needsOnboarding, loadingPrefs, etc).
- Keep all tabs working: home/upload/wardrobe/stylist/planner/profile.
- Keep existing imports unless explicitly asked to remove (do NOT remove Onboarding import).
- Only add the onboarding modal UI + gating glue.

GOAL:
Add a premium onboarding modal overlay (3 steps) and show it when:
user is logged in AND loadingPrefs === false AND needsOnboarding === true.

────────────────────────────────────────────────────────
A) ADD STATE (SEARCH → INSERT)
SEARCH for this exact line inside App.jsx:

const [todayPlan, setTodayPlan] = useState({ outfit: { items: [] } });

INSERT IMMEDIATELY AFTER it (new lines):

  // ── Onboarding modal UI state (frontend-only) ──
  const [onboardingOpen, setOnboardingOpen] = useState(false);
  const [onboardingStep, setOnboardingStep] = useState(0);

────────────────────────────────────────────────────────
B) ADD GATING useEffect (SEARCH → INSERT)
SEARCH for this exact block start (it exists in your file):

useEffect(() => {
    if (!user?.uid) return;

INSERT IMMEDIATELY BEFORE that entire useEffect block (above it) this new useEffect:

  // ── Frontend-only onboarding gate (opens modal when prefs are missing) ──
  useEffect(() => {
    // if user logs out, close/reset
    if (!user?.uid) {
      setOnboardingOpen(false);
      setOnboardingStep(0);
      return;
    }

    // open modal only after prefs are done loading
    if (!loadingPrefs && needsOnboarding) {
      setOnboardingOpen(true);
      setOnboardingStep(0);
    }

    // if onboarding becomes not-needed, close modal
    if (!loadingPrefs && !needsOnboarding) {
      setOnboardingOpen(false);
    }
  }, [user?.uid, loadingPrefs, needsOnboarding]);

NOTE:
Do NOT remove your existing onboarding fetch useEffect. Keep it exactly as-is.

────────────────────────────────────────────────────────
C) ADD MODAL COMPONENT (SEARCH → INSERT)
SEARCH for this exact line in App.jsx:

function LoadingState({ text = "Loading…" }) {

SCROLL DOWN to the end of LoadingState component (right after its closing brace).
You should see:

}

INSERT IMMEDIATELY AFTER LoadingState’s closing brace (new component below it):

function OnboardingModal({
  open,
  step,
  onClose,
  onNext,
  onGoUpload,
  onGoStylist,
}) {
  if (!open) return null;

  const steps = [
    {
      title: "Welcome to WOW",
      subtitle: "Less thinking. Better outfits.",
      primary: "Get started →",
      onPrimary: onNext,
    },
    {
      title: "Let’s start with your wardrobe",
      subtitle: "Upload 5–10 items you wear often. That’s enough for WOW to work.",
      helper: "Takes about 2 minutes",
      primary: "Upload items →",
      onPrimary: onGoUpload,
    },
    {
      title: "This is how WOW works",
      subtitle: "Add your clothes → See what you own → Get dressed without thinking",
      primary: "Style me for today →",
      onPrimary: onGoStylist,
    },
  ];

  const s = steps[Math.max(0, Math.min(step, steps.length - 1))];

  // Esc closes
  useEffect(() => {
    const onKey = (e) => {
      if (e.key === "Escape") onClose?.();
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [onClose]);

  return (
    <div className="wow-onb-overlay" role="dialog" aria-modal="true" onClick={onClose}>
      <div className="wow-onb-card" onClick={(e) => e.stopPropagation()}>
        <button className="wow-onb-close" type="button" onClick={onClose} aria-label="Close onboarding">
          ✕
        </button>

        <div className="wow-onb-progress" aria-label="Onboarding progress">
          {[0, 1, 2].map((i) => (
            <span key={i} className={`wow-onb-dot ${i === step ? "is-active" : ""}`} />
          ))}
          <span className="wow-onb-count">{step + 1}/3</span>
        </div>

        <h2 className="wow-onb-title">{s.title}</h2>
        <p className="wow-onb-subtitle">{s.subtitle}</p>
        {s.helper ? <p className="wow-onb-helper">{s.helper}</p> : null}

        <div className="wow-onb-actions">
          <button className="wow-onb-primary" type="button" onClick={s.onPrimary}>
            {s.primary}
          </button>

          <button className="wow-onb-skip" type="button" onClick={onClose}>
            Skip
          </button>
        </div>
      </div>
    </div>
  );
}

IMPORTANT:
- Do NOT move LoadingState.
- Keep this component exactly below it.

────────────────────────────────────────────────────────
D) INSERT MODAL RENDER (SEARCH → INSERT)
SEARCH for this exact line in your return JSX:

<header className="app-header">

Scroll down to find the closing </header> of that header section.

Immediately AFTER the </header> line, INSERT this modal render block:

      <OnboardingModal
        open={!!user && onboardingOpen}
        step={onboardingStep}
        onClose={() => setOnboardingOpen(false)}
        onNext={() => setOnboardingStep((s) => Math.min(2, s + 1))}
        onGoUpload={() => {
          setOnboardingOpen(false);
          setActiveTab("upload");
        }}
        onGoStylist={() => {
          setOnboardingOpen(false);
          setActiveTab("stylist");
        }}
      />

NOTE:
- This block must be inside the top-level <div className="app">, but outside <main>.
- Do NOT replace existing header content.

────────────────────────────────────────────────────────
E) ADD CSS (App.css) (SEARCH → APPEND)
Open App.css and APPEND these styles at the bottom:

/* =========================
   WOW Onboarding Modal
   ========================= */
.wow-onb-overlay{
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.62);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 3000;
  padding: 18px;
  backdrop-filter: blur(6px);
}

.wow-onb-card{
  width: min(520px, 100%);
  background: rgba(18,18,18,0.92);
  border: 1px solid rgba(255,255,255,0.10);
  border-radius: 20px;
  padding: 22px 20px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.55);
  transform: translateY(6px);
  animation: wowOnbIn 180ms ease-out forwards;
  position: relative;
}

@keyframes wowOnbIn{
  from { opacity: 0; transform: translateY(14px); }
  to   { opacity: 1; transform: translateY(6px); }
}

.wow-onb-close{
  position: absolute;
  top: 12px;
  right: 12px;
  height: 36px;
  width: 36px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,0.12);
  background: rgba(255,255,255,0.06);
  color: #fff;
  cursor: pointer;
}

.wow-onb-progress{
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 14px;
}

.wow-onb-dot{
  width: 8px;
  height: 8px;
  border-radius: 99px;
  background: rgba(255,255,255,0.18);
}

.wow-onb-dot.is-active{
  background: rgba(255,255,255,0.85);
}

.wow-onb-count{
  margin-left: auto;
  font-size: 12px;
  opacity: 0.75;
  color: #fff;
}

.wow-onb-title{
  margin: 0;
  font-size: 22px;
  line-height: 1.2;
  color: #fff;
  letter-spacing: 0.2px;
}

.wow-onb-subtitle{
  margin: 10px 0 0;
  font-size: 14.5px;
  line-height: 1.5;
  color: rgba(255,255,255,0.82);
}

.wow-onb-helper{
  margin: 10px 0 0;
  font-size: 13px;
  color: rgba(255,255,255,0.65);
}

.wow-onb-actions{
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-top: 18px;
}

.wow-onb-primary{
  width: 100%;
  height: 46px;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,0.12);
  background: #ffffff;
  color: #111;
  font-weight: 700;
  cursor: pointer;
}

.wow-onb-skip{
  width: 100%;
  height: 44px;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,0.14);
  background: rgba(255,255,255,0.06);
  color: #fff;
  cursor: pointer;
}

.wow-onb-primary:active,
.wow-onb-skip:active,
.wow-onb-close:active{
  transform: translateY(1px);
}

────────────────────────────────────────────────────────
F) FINAL CHECKS (MANDATORY)
1) App should compile with no missing imports.
2) Modal must appear only for logged-in users when needsOnboarding is true (after loadingPrefs false).
3) Step 2 CTA must switch tab to "upload".
4) Step 3 CTA must switch tab to "stylist".
5) Skip/Close/Escape/Outside click must close modal without tab change.
6) Do NOT render the old <Onboarding /> component anywhere.

NOW ASK ME FOR App.jsx ONLY IF YOU NEED IT — otherwise apply edits using search/insert.

When done, output:
- the exact snippets you inserted (state, useEffect, component, JSX insertion)
- the CSS you added
- and confirm you did not touch backend logic.
